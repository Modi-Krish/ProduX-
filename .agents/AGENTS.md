# Project Rules

* **Mobile First & Capacitor Optimization**: Always optimize all code changes for native mobile applications (Capacitor/Android/iOS). Ensure network requests dynamically handle local vs production hostnames, native push notifications (FCM) are supported, UI is fully responsive with touch-friendly elements, and native platform features are safely conditionalized.
